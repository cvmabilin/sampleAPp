import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    error: {
        color: 'red', 
        fontSize: 14
    },
    form: {
        flex: 3.4
    },
    commands: {
        flex: 1
    }
});